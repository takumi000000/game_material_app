<div id="chat">
  <% @chat_messages.each do |chat_message| %>
    <p><%= chat_message.content %></p>
    <p><%= chat_message.created_at.strftime("%H:%M") %></p>
  <% end %>
</div>
<form>
  投稿: <input type="text" />
  <input type="submit" value="投稿" onClick="postChatMessage()" />
</form>